[build]
  cmd = "go build -o ./tmp/cloudcontrol ./cmd/server"
  bin = "./tmp/cloudcontrol"
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin"]
  delay = 1000

[log]
  time = true

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  runner = "magenta"
